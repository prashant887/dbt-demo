version: 2

seeds:
  - name: locations
    description: A mapping of two letter country codes to country names
    columns:
      - name: city
        tests:
          - unique
          - not_null
      - name: pincode
        tests:
          - unique
          - not_null